<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Todo6133261.Views.TodoItemPage"
             Title="{Binding Name}"
             xmlns:models="clr-namespace:Todo6133261.Models"
             x:DataType="models:TodoItem">
    <ScrollView>
        <VerticalStackLayout Padding="20"
                             Spacing="10"
                             VerticalOptions="StartAndExpand">
        <Label Text="Name:"/>
        <Entry Text="{Binding Name}"/>
        <Label Text="Notes:"/>
        <Entry Text="{Binding Notes}"/>
        <Label Text="Done:"/>
        <Switch IsToggled="{Binding Done}"/>
        <Button Text="Save" Clicked="OnSaveClicked"></Button>
        <Button Text="Delete" Clicked="OnDeleteClicked"></Button>
        <Button Text="Cancel" Clicked="OnCancelClicked"></Button>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>